# Kubeconfig (contains cluster admin credentials)
kubeconfig/

# Repo-local SSH keys (emergency access)
# Keep the directory tracked (via .gitkeep) but ignore everything else.
/.ssh/*
!/.ssh/.gitkeep

# Terraform state (if using local backend)
*.tfstate
*.tfstate.*

# Terraform directories
**/.terraform/*

# Sensitive variable files
*.tfvars
*.tfvars.json

# OS files
.DS_Store
